// The plugin consumes `@loontail/minecraft-kit` directly from the controller,
// so no Strapi-level services need to be registered. Strapi still expects
// this file to default-export a service map; we export an empty one.
export default {};
